; Chunk 1 of the desert's rear layer, which LMPR pages in at 0x0000: a
; stretch of the band's rows, each compiled at four pixel phases, with the
; table of entry points a rotation is reached through. C9_RET comes from
; the resident assembly: a run's way back is all the bank has to know.
        DEVICE NOSLOT64K
        INCLUDE "desert9run1.z80s"
