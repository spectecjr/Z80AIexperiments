                ORG 32768
start:
                INCLUDE "8x8multiply_r16.z80s"
                INCLUDE "float16.z80s"
                INCLUDE "float32.z80s"
                INCLUDE "crc32.z80s"
                INCLUDE "murmur3.z80s"
                INCLUDE "tab64.z80s"
                INCLUDE "mul8x8_qs.z80s"
                INCLUDE "transform3d.z80s"
                INCLUDE "democube.z80s"
                INCLUDE "render.z80s"
end:            equ $
codelen:        equ end - start
